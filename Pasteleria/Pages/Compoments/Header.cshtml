@page "/Header"
@model Pasteleria.Pages.Compoments.HeaderModel
@using Pasteleria.Models

@{
	var url = Url.ActionLink();
	Menu menu = new Menu();

	if (url.EndsWith("/") || url.Contains("Index"))
	{
		menu = Resources.menu.First(x => x.url == "Index");

	} else
	{
		foreach (var item in Resources.menu)
		{
			if (url.Contains(item.url))
			{
				menu = Resources.menu.First(x => x.url == item.url);
				break;
			}
		}
	}
}


<!-- HEADER =============================-->
<header class="item header margin-top-0">
<div class="wrapper">
	<nav role="navigation" class="navbar navbar-white navbar-embossed navbar-lg navbar-fixed-top">
	<div class="container">
		<div class="navbar-header">
			<button data-target="#navbar-collapse-02" data-toggle="collapse" class="navbar-toggle" type="button">
			<i class="fa fa-bars"></i>
			<span class="sr-only">Toggle navigation</span>
			</button>
			<a asp-area="" asp-page="/" class="navbar-brand brand"> @(Resources.appName) </a>
			<small> @(Resources.appVersion) </small>
		</div>
		<div id="navbar-collapse-02" class="collapse navbar-collapse">
			<ul class="nav navbar-nav navbar-right">

				@foreach(var menuOptions in Resources.menu){
					<li class="propClone"><a asp-area="" asp-page="@menuOptions.url">@menuOptions.name</a></li>
				}

			</ul>
		</div>
	</div>
	</nav>
	<div class="container">
		<div class="row">
			<div class="col-md-12 text-center">
				
				@if (!String.IsNullOrWhiteSpace(menu.title2))
				{
					<div class="text-homeimage">
						<div class="maintext-image" data-scrollreveal="enter top over 1.5s after 0.1s">
							 @menu.title1
						</div>
						<div class="subtext-image" data-scrollreveal="enter bottom over 1.7s after 0.3s">
							 @menu.title2
						</div>
					</div>	

				}else{
					<div class="text-pageheader">
						<div class="subtext-image" data-scrollreveal="enter bottom over 1.7s after 0.1s">
							 @menu.title1
						</div>
					</div>
				}
			</div>
		</div>
	</div>
</div>
</header>
